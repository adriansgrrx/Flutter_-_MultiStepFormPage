name: smart_ledger_ai
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dots_indicator: ^3.0.0
  google_fonts: ^5.1.0
  image_picker: ^0.8.4+2
  flutter_custom_clippers: ^2.0.0
  flutter_speed_dial:
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
